{
  "name": "waffle-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:sol": "waffle",
    "build:ts": "tsc",
    "build": "yarn build:sol && yarn build:ts",
    "flatten": "waffle flatten",
    "test": "mocha --timeout 10000 -r ts-node/register/transpile-only 'test/**/*.test.ts' "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openzeppelin/contracts": "^4.0.0",
    "@types/chai": "^4.2.16",
    "@types/mocha": "^8.2.2",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.3.0",
    "mocha": "^8.3.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
